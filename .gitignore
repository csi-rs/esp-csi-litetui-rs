# Rust build output
/target/
**/*.rs.bk

# Note: Cargo.lock is intentionally committed — this is a firmware binary and
# we want reproducible builds.

# Local tool caches
/.cachebro/

# Editor / OS noise
/.vscode/
/.idea/
*.swp
*.swo
*~
.DS_Store

# CSI captures pulled off the SD card (data, not source)
/captures/
*.BIN
*.CSV
