[build]
target = "xtensa-esp32s3-none-elf"

[target.xtensa-esp32s3-none-elf]
runner = "espflash flash --monitor"
rustflags = [
    "-C", "link-arg=-Tlinkall.x",
]
[unstable]
# This requires Rust nightly. It builds the core memory allocator from scratch.
build-std = ["core", "alloc"]